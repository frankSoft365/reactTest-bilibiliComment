import { Button } from "antd-mobile";

export default function Month() {
    return (
        <div>
            this is month
            <Button color="primary">click here</Button>
        </div>
    );
}